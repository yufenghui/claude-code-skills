# 8X Flow 产出图固化 实现计划

> **For agentic workers:** 用 superpowers:executing-plans 逐任务执行。步骤用 checkbox 跟踪。

**Goal:** 把凭证追溯图、变化点图、最终业务模型全视图固化进 `bm-8x-flow` 的产出章节序列。

**Architecture:** 纯文档修改——改 `SKILL.md`（5 处）+ `live-session.md`（1 处），无代码 / 无测试。每处用 Edit 精确替换，验证靠读回 + 章节计数 + 交叉引用一致。

**Tech Stack:** Markdown（skill 文档）。

**关联 spec:** `docs/superpowers/specs/2026-06-30-8x-flow-output-diagrams-design.md`

---

## File Structure

| 文件 | 责任 | 改动 |
|------|------|------|
| `packages/business-modeling/skills/bm-8x-flow/SKILL.md` | 8X Flow 引导主体 | 5 处（运行方式说明、进度清单、步骤 2/3 正文、产出列表） |
| `packages/business-modeling/reference/live-session.md` | 增量引导 + live server 运行方式 | 1 处（§4 末尾两 skill 步骤映射） |

不改：`output-format.md`（"业务模型图（全景图）"要求已正确，本次只是让 8X Flow 落实它）、`concepts.md`、`bm-four-color/SKILL.md`（凭证链图画法定义在此，8X Flow 引用）。

---

## Task 1：SKILL.md 运行方式说明 + 五步法进度清单

**Files:** Modify `SKILL.md:17`、`SKILL.md:53-60`

- [ ] **Step 1：改第 17 行运行方式段落**

old（精确匹配第 17 行）：
```
8X Flow 的五步法对应章节序列的：第 4 章替换为"合同上下文图 + 主要履约项表"、第 5 章替换为"违约流转图"，并加"弹性边界 / 服务图"与"变化点清单"。
```
new：
```
8X Flow 的五步法对应章节序列的：第 4 章替换为"合同上下文图 + 主要履约项表 + 凭证追溯图"、第 5 章替换为"违约流转图"，并加"变化点图"、"弹性边界 / 服务图"与"最终业务模型全视图"（收尾整合）。
```

- [ ] **Step 2：改第 54–60 行五步法进度清单**

old（精确匹配代码块内容）：
```
8X Flow 进度（每步产出一章，增量追加进 report.html）：
- [ ] 1. 找合同上下文，明确参与方 → 合同上下文图
- [ ] 2. 找主要履约项，按四色找凭证 → 主要履约项表
- [ ] 3. 找违约情况，设新履约项，按四色找凭证 → 违约流转图
- [ ] 4. 重复步骤 3，直到不得不打官司为止 → 违约流转图（补全）
- [ ] 5. 把参与方与标的物划入领域边界 → 弹性边界 / 服务图
```
new：
```
8X Flow 进度（每步产出一章，增量追加进 report.html）：
- [ ] 1. 找合同上下文，明确参与方 → 合同上下文图
- [ ] 2. 找主要履约项，按四色找凭证 → 主要履约项表 + 凭证追溯图
- [ ] 3. 找违约情况，设新履约项，按四色找凭证 → 违约流转图
- [ ] 4. 重复步骤 3，直到不得不打官司为止 → 违约流转图（补全）
- [ ] 5. 把参与方与标的物划入领域边界 → 弹性边界 / 服务图
- [ ] 收尾：整合所有合同上下文 + 凭证串联 + 领域系统 → 最终业务模型全视图
```

- [ ] **Step 3：验证** — 读回 SKILL.md 第 17 行与第 53–61 行，确认替换成功、代码块闭合。

---

## Task 2：SKILL.md 步骤 2 / 步骤 3 正文

**Files:** Modify `SKILL.md:64`、`SKILL.md:66`

- [ ] **Step 1：改第 64 行步骤 2 正文（找凭证子步骤③补充产出 + 深度 + 边界）**

old（精确匹配第 64 行末段）：
```
**找凭证的最小子步骤**（四色法核心）：① 围绕该履约项找现金 / KPI 凭证；② 列关键数据项（时间点 + 金额）；③ 追溯每个关键数据项的来源。若需完整凭证链，调用 `bm-four-color`。
```
new：
```
**找凭证的最小子步骤**（四色法核心）：① 围绕该履约项找现金 / KPI 凭证；② 列关键数据项（时间点 + 金额）；③ 追溯每个关键数据项的来源（前序凭证数值 / 人工输入 / 规则计算，见 `../../reference/concepts.md` 的"关键数据项的三类来源"），**产出凭证追溯图**（凭证链图 + 凭证表，复用 `bm-four-color` 画法：实线 = 数值 / 时标来源，虚线 = 规则来源）。追溯范围限定**主要履约项的核心凭证链**；违约履约项凭证在步骤 3 的违约流转图节点上标注，不入此图。若需完整全量凭证链，调用 `bm-four-color`。
```

- [ ] **Step 2：改第 66 行步骤 3 正文（末尾补违约凭证标注说明）**

old（精确匹配第 66 行末段）：
```
违约责任也需合同预先覆盖。按四色找凭证。
```
new：
```
违约责任也需合同预先覆盖。按四色找凭证，**违约履约项的凭证在违约流转图节点上标注**（不单独追溯，见步骤 2 边界）。
```

- [ ] **Step 3：验证** — 读回 SKILL.md 第 64、66 行，确认子步骤③与步骤 3 末尾已更新。

---

## Task 3：SKILL.md 产出列表（核心改动）

**Files:** Modify `SKILL.md:99-107`

- [ ] **Step 1：替换产出列表第 1–8 项**

old（精确匹配第 99–107 行的 8 项）：
```
1. **合同上下文图**：参与方 + 双方合同，**标出平台 / 枢纽方的对手方结算合同**（粗线）与仅撮合的合同区分。
2. **主要履约项表**：每项权利方 / 义务方 + 履约请求（时间段）/ 履约确认（时间点）+ 凭证。
3. **违约流转图**：每个违约 → 独立履约项 vs 自动作废的判别路径，终止于打官司。
4. **变化点清单**：角色化的履约确认、不同渠道上下文、不同标的物来源。
5. **弹性边界 / 服务图**：按履约上下文划分弹性边界 → 服务映射（区分业务系统 / 领域系统）。
6. **端到端走查**：正常流程 + 至少 1 个违约场景，验证模型自洽。
7. **业务组件设计**（可选延伸）：领域组件清单、业务组件清单、组件划分总图、组件调用时序图——见下文「业务组件设计」。
8. **中台化：宏流程提取**（可选延伸）：宏流程图 + 中台业务模型——仅当目标是下沉中台 / 多前台复用时产出，见下文「中台化」。
```
new：
```
1. **合同上下文图**：参与方 + 双方合同，**标出平台 / 枢纽方的对手方结算合同**（粗线）与仅撮合的合同区分。
2. **主要履约项表**：每项权利方 / 义务方 + 履约请求（时间段）/ 履约确认（时间点）+ 凭证。
3. **凭证追溯图**：主要履约项的核心凭证链——追溯每个关键数据项来源（前序凭证数值 / 人工输入 / 规则计算），凭证链图（实线 = 数值 / 时标来源，虚线 = 规则来源）+ 凭证表（计算逻辑 / 时标顺序逻辑）。复用 `bm-four-color` 画法；完整全量凭证链调 `bm-four-color`。
4. **违约流转图**：每个违约 → 独立履约项 vs 自动作废的判别路径，终止于打官司；违约履约项凭证在节点上标注。
5. **变化点图**：角色化履约确认图（哪些履约确认被角色化、由哪些合同凭证扮演）为主 + 关键变化点表（变化点 / 角色化的履约确认 / 扮演凭证来源合同 / 业务意义）为辅。
6. **弹性边界 / 服务图**：按履约上下文划分弹性边界 → 服务映射（区分业务系统 / 领域系统）。
7. **最终业务模型全视图**：整合所有合同上下文 + 凭证跨上下文串联 + 领域系统（标的物归领域系统，经凭证引用参与业务），呈现业务如何将领域系统转化为可盈利产品。单合同场景即该合同完整模型；多合同场景为多上下文整合全景。落实 `../../reference/output-format.md` "业务模型图（全景图）"要求。
8. **端到端走查**：正常流程 + 至少 1 个违约场景，验证模型自洽。
9. **业务组件设计**（可选延伸）：领域组件清单、业务组件清单、组件划分总图、组件调用时序图——见下文「业务组件设计」。
10. **中台化：宏流程提取**（可选延伸）：宏流程图 + 中台业务模型——仅当目标是下沉中台 / 多前台复用时产出，见下文「中台化」。
```

- [ ] **Step 2：验证** — 读回 SKILL.md 产出列表，确认：从 8 项变 10 项；第 3 项为凭证追溯图、第 5 项为变化点图、第 7 项为最终业务模型全视图、端到端走查在第 8。

---

## Task 4：live-session.md 两 skill 步骤映射

**Files:** Modify `live-session.md:61`

- [ ] **Step 1：改第 61 行映射说明**

old（精确匹配第 61 行）：
```
> 两个 skill 的步骤映射：`bm-four-color` 的 7 步对应 3–7 章（凭证链为主）；`bm-8x-flow` 的五步法把第 4 章换成"合同上下文图 + 主要履约项表"、第 5 章换成"违约流转图"、并加"弹性边界/服务图"与"变化点清单"。具体见各自 SKILL.md。
```
new：
```
> 两个 skill 的步骤映射：`bm-four-color` 的 7 步对应 3–7 章（凭证链为主）；`bm-8x-flow` 的五步法把第 4 章换成"合同上下文图 + 主要履约项表 + 凭证追溯图"、第 5 章换成"违约流转图"、并加"变化点图"、"弹性边界/服务图"与"最终业务模型全视图"。具体见各自 SKILL.md。
```

- [ ] **Step 2：验证** — 读回 live-session.md 第 61 行，确认映射已含三张新图。

---

## Task 5：整体验证 + 开分支 + 提交

- [ ] **Step 1：交叉一致性检查** — grep `SKILL.md` 与 `live-session.md`，确认"变化点清单"已全部改为"变化点图"；"凭证追溯图""最终业务模型全视图"在两文件表述一致。
- [ ] **Step 2：开分支**（当前在 master，按 harness 规则先开分支）：`git switch -c feat/8x-flow-output-diagrams`
- [ ] **Step 3：提交**：`git add` 改动的 2 个 skill 文件 + spec + plan，commit message 遵循仓库中文约定（`feat(business-modeling): ...`）。
- [ ] **Step 4：报告** — 列出改动文件、commit hash、章节序列终态。

---

## 自审（writing-plans Self-Review）

- **Spec 覆盖**：spec 第二节 6 项决策 → Task 1（进度清单 + 运行说明）、Task 2（步骤 2 追溯深度/边界 + 步骤 3 违约凭证）、Task 3（产出列表：凭证追溯图/变化点图/全视图/走查位置）、Task 4（live-session 映射）。spec 第五节"需改动文件"全部覆盖。✓
- **占位符扫描**：每步均含完整 old/new 文本，无 TBD/TODO。✓
- **一致性**：章节编号在进度清单（Task 1）、产出列表（Task 3）、live-session 映射（Task 4）三处表述一致——凭证追溯图属步骤 2、变化点图、全视图收尾。✓
